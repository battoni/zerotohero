<template>
  <div class="flex flex-col gap-6 pt-6">
    <h1 class="text-3xl font-black">{{ $t('tracks.title') }}</h1>
    <p class="text-muted" data-testid="tracks-empty">{{ $t('tracks.empty') }}</p>
  </div>
</template>
